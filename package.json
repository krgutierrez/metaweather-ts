{
  "name": "meta-weather-ts",
  "description": "A meta weather client api using typescript",
  "version": "0.0.1",
  "scripts": {
    "test": "jest",
    "build": "tsc"
  },
  "dependencies": {
    "axios": "^0.18.0",
    "typescript": "^3.4.3"
  },
  "devDependencies": {
    "@types/jest": "^24.0.11",
    "jest": "^24.7.1",
    "ts-jest": "^24.0.2"
  }
}
